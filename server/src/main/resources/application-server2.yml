server:
  port: 9002
spring:
  application:
    name: server
eureka:
#  instance:
#    hostname: localhost
  client:
#    registerWithEureka: false
    serviceUrl:
      defaultZone: http://localhost:9001/eureka/
  server:
    enable-self-preservation: false